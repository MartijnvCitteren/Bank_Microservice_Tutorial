server:
  port:8080
spring:
  datasource:
    url: jdbc:h2:mem:/testdb/bank
    driveClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
      show-sql: true
  config:
    import:
      - "application_qa.yml"
      - "application_prod.yml"
  profiles:
    active:
      - "prod"



build:
  version: "3.0"

accounts:
  message: "Welcome to EasyBank Accounts in the Devlopment environment"
  contactDetails:
    name: "John doe"
    email: "john@mailme.com"
  onCallSupport:
    - 123-456-7890
    - 123-456-7811